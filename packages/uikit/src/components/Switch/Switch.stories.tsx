/* import React from 'react';
import { Story, Meta } from '@storybook/react';
import Switch from './Switch';

export default {
  title: 'Components/Switch',
  component: Switch,
  argTypes: {
     ตอนนี้มีไซส์เดียวไปก่อน
     size: {
       name: 'size',
       options: ['sm', 'md', 'lg'],
       control: { type: 'select' },
       defaultValue: 'md',
     },
    checked: {
      name: 'checked',
      control: { type: 'boolean' },
      defaultValue: true,
    },
  },
} as Meta;
export const Default: Story = ({ checked }) => <Switch checked={checked} />; */
